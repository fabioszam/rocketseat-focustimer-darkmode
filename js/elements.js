const btnLight = document.querySelector(".btn-light");
const btnDark = document.querySelector(".btn-dark");
const minutesDisplay = document.querySelector(".minutes");
const secondsDisplay = document.querySelector(".seconds");
const buttonPlay = document.querySelector(".play");
const buttonPause = document.querySelector(".pause");
const buttonStop = document.querySelector(".stop");
const buttonPlus = document.querySelector(".plus");
const buttonMinus = document.querySelector(".minus");
const buttonForest = document.querySelector(".button-forest");
const buttonForestSelected = document.querySelector(".button-forest-selected");
const buttonRain = document.querySelector(".button-rain");
const buttonRainSelected = document.querySelector(".button-rain-selected");
const buttonCoffee = document.querySelector(".button-coffee");
const buttonCoffeeSelected = document.querySelector(".button-coffee-selected");
const buttonFireplace = document.querySelector(".button-fireplace");
const buttonFireplaceSelected = document.querySelector(
  ".button-fireplace-selected"
);
const sliderForest = document.querySelector(".slider-forest");
const sliderForestSelected = document.querySelector(".slider-forest-selected");
const sliderRain = document.querySelector(".slider-rain");
const sliderRainSelected = document.querySelector(".slider-rain-selected");
const sliderCoffee = document.querySelector(".slider-coffee");
const sliderCoffeeSelected = document.querySelector(".slider-coffee-selected");
const sliderFireplace = document.querySelector(".slider-fireplace");
const sliderFireplaceSelected = document.querySelector(
  ".slider-fireplace-selected"
);

export {
  btnLight,
  btnDark,
  minutesDisplay,
  secondsDisplay,
  buttonPlay,
  buttonPause,
  buttonStop,
  buttonPlus,
  buttonMinus,
  buttonForest,
  buttonForestSelected,
  buttonRain,
  buttonRainSelected,
  buttonCoffee,
  buttonCoffeeSelected,
  buttonFireplace,
  buttonFireplaceSelected,
  sliderForest,
  sliderForestSelected,
  sliderRain,
  sliderRainSelected,
  sliderCoffee,
  sliderCoffeeSelected,
  sliderFireplace,
  sliderFireplaceSelected,
};
